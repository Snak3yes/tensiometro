{
  "name": "conductor-update",
  "description": "Update Conductor base documentation (product, tech-stack, workflow) from setup responses",
  "author": "Claude Sonnet 4.5",
  "parameters": {
    "doc": {
      "type": "string",
      "description": "Document to update",
      "enum": ["product", "tech-stack", "workflow", "all"],
      "default": "all"
    },
    "confirm": {
      "type": "boolean",
      "description": "Skip confirmation prompt",
      "default": false
    }
  }
}
