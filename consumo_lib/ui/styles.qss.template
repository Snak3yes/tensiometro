/* =============================================================================
   ⚠️⚠️⚠️ REGRA CRÍTICA: PROIBIÇÃO DE ESTILOS INLINE/HARDCODED ⚠️⚠️⚠️

   ESTE ARQUIVO É A ÚNICA FONTE DE VERDADE PARA ESTILOS QSS/GLOBAIS.

   ❌ COMPLETAMENTE PROIBIDO em qualquer lugar da aplicação:
      - .setStyleSheet() inline em código Python
      - Valores hardcoded de cores (#4CAF50, #e74c3c, etc.)
      - Valores hardcoded de fontes (14px, bold, etc.)
      - Valores hardcoded de espaçamento (8px, 16px, etc.)
      - Qualquer estilo que NÃO esteja centralizado aqui

   ✅ SEMPRE USE ESTE ARQUIVO PARA ESTILOS GLOBAIS:
      - Defina estilos globais AQUI usando placeholders {{TOKEN}}
      - Placeholders são substituídos por design_tokens.py em runtime
      - Para estilos específicos de componentes, use widget_standards.py

   ✅ SE PRECISA DE ESTILO NOVO:
      - Adicione ESTE arquivo usando placeholder {{TOKEN}}
      - Crie token correspondente em design_tokens.py
      - Documente o propósito do estilo

   ⚠️ ESTA REGRA NÃO PODE SER BURLADA - Code review irá rejeitar violações
   ⚠️⚠️⚠️ FIM DA REGRA CRÍTICA ⚠️⚠️⚠️
   ============================================================================= */

/**
 * Qt Style Sheet Global - Tensiometro
 *
 * Segue Material Design 3 principles
 * Usa design tokens definidos em design_tokens.py
 *
 * Última atualização: 2026-01-20
 * Versão: 2.1.0 - Cores v2.1: WCAG AA compliance + Minimalismo Industrial
 */

/* =============================================================================
   RESET & BASE
   ============================================================================= */

* {
    outline: none;
}

QWidget {
    background-color: {{BACKGROUND}};
    color: {{TEXT_PRIMARY}};
    font-family: Arial;
    font-size: 12px;
}

/* =============================================================================
   BUTTONS
   ============================================================================= */

QPushButton {
    background-color: {{PRIMARY}};
    color: white;
    border: none;
    border-radius: 4px;
    padding: 8px 16px;
    font-size: 14px;
    font-weight: bold;
    min-height: 40px;
}

QPushButton:hover {
    background-color: {{PRIMARY_DARK}};
}

QPushButton:pressed {
    background-color: {{PRIMARY_LIGHT}};
}

QPushButton:disabled {
    background-color: {{BORDER}};
    color: {{TEXT_DISABLED}};
}

/* Button Variants (via property) */

/* Primary Green (ações de confirmação/início) - v2.1 */
QPushButton[variant="primary-green"] {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                                stop:0 #43A047, stop:1 #2E7D32);  /* Green 700-800 */
    border: 2px solid #2E7D32;  /* Green 800 */
    color: white;
    border-radius: 10px;
    font-weight: 500;
}

QPushButton[variant="primary-green"]:hover {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                                stop:0 #66BB6A, stop:1 #43A047);  /* Green 400-700 */
}

/* Primary Blue (ações padrão/genéricas) - v2.1 */
QPushButton[variant="primary-blue"] {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                                stop:0 #455A64, stop:1 #37474F);  /* Blue Grey 700-800 */
    border: 2px solid #37474F;  /* Blue Grey 800 */
    color: white;
    border-radius: 10px;
    font-weight: 500;
}

QPushButton[variant="primary-blue"]:hover {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                                stop:0 #607D8B, stop:1 #455A64);  /* Blue Grey 500-700 */
}

/* Primary Orange (ações de parada/atenção) - v2.1 */
QPushButton[variant="primary-orange"] {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                                stop:0 #E65100, stop:1 #BF360C);  /* Orange 900-900 */
    border: 2px solid #BF360C;  /* Orange 900 */
    color: white;
    border-radius: 10px;
    font-weight: 500;
}

QPushButton[variant="primary-orange"]:hover {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                                stop:0 #FF9800, stop:1 #E65100);  /* Orange 500-900 */
}

/* Secondary (outline - ações alternativas/cancelamento) - v2.1 */
QPushButton[variant="secondary"] {
    background-color: transparent;
    border: 2px solid #455A64;  /* Blue Grey 700 */
    color: #455A64;
    border-radius: 5px;
    font-weight: 500;
}

QPushButton[variant="secondary"]:hover {
    background-color: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                                stop:0 #607D8B, stop:1 #455A64);  /* Blue Grey 500-700 */
    color: white;
}

QPushButton[variant="secondary"]:disabled {
    background-color: transparent;
    border: 2px solid {{BORDER}};
    color: {{TEXT_DISABLED}};
}

/* Emergency (emergências físicas - uso raro 1%) - v2.1 */
QPushButton[variant="emergency"] {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                                stop:0 #C62828, stop:1 #B71C1C);  /* Red 800-900 */
    border: 3px solid #B71C1C;  /* Red 900 */
    color: white;
    border-radius: 25px;
    padding: 15px 40px;
    font-weight: 700;
    font-size: 16px;
}

QPushButton[variant="emergency"]:hover {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                                stop:0 #E53935, stop:1 #C62828);  /* Red 700-800 */
}

QPushButton[variant="emergency"]:disabled {
    background-color: {{BORDER}};
    border: 3px solid {{BORDER_DARK}};
    color: {{TEXT_DISABLED}};
}

/* Danger (DEPRECATED - use emergency instead) */
QPushButton[variant="danger"] {
    background-color: {{ERROR}};
}

QPushButton[variant="danger"]:hover {
    background-color: {{ERROR_DARK}};
}

QPushButton[variant="danger"]:pressed {
    background-color: {{ERROR_DARK}};
}

/* Outline (borda verde - legado, use secondary no futuro) */
QPushButton[variant="outline"] {
    background-color: transparent;
    border: 2px solid {{PRIMARY}};
    color: {{PRIMARY}};
}

QPushButton[variant="outline"]:hover {
    background-color: {{PRIMARY}};
}

/* =============================================================================
   TEXT & LABELS
   ============================================================================= */

QLabel {
    color: {{TEXT_PRIMARY}};
    background-color: transparent;
}

QLabel[heading="true"] {
    font-size: 16px;
    font-weight: bold;
    color: {{TEXT_PRIMARY}};
}

QLabel[subheading="true"] {
    font-size: 14px;
    font-weight: bold;
    color: {{TEXT_SECONDARY}};
}

QLabel[caption="true"] {
    font-size: 11px;
    color: {{TEXT_DISABLED}};
}

/* =============================================================================
   INPUTS
   ============================================================================= */

QLineEdit, QTextEdit, QPlainTextEdit {
    background-color: {{BACKGROUND}};
    border: 2px solid {{BORDER}};
    border-radius: 4px;
    padding: 8px;
    color: {{TEXT_PRIMARY}};
    min-height: 40px;
}

QLineEdit:focus, QTextEdit:focus, QPlainTextEdit:focus {
    border-color: {{SECONDARY}};
}

QLineEdit:disabled, QTextEdit:disabled, QPlainTextEdit:disabled {
    background-color: {{SURFACE_VARIANT}};
    color: {{TEXT_DISABLED}};
}

QLineEdit:hover, QTextEdit:hover, QPlainTextEdit:hover {
    border-color: {{BORDER_DARK}};
}

/* =============================================================================
   COMBO BOX
   ============================================================================= */

QComboBox {
    background-color: {{BACKGROUND}};
    border: 2px solid {{BORDER}};
    border-radius: 4px;
    padding: 8px;
    min-height: 40px;
}

QComboBox:hover {
    border-color: {{BORDER_DARK}};
}

QComboBox::drop-down {
    border: none;
}

QComboBox::down-arrow {
    width: 12px;
    height: 12px;
}

QComboBox QAbstractItemView {
    background-color: {{BACKGROUND}};
    border: 2px solid {{BORDER}};
    selection-background-color: {{SECONDARY}};
    selection-color: {{TEXT_PRIMARY}};
    padding: 4px;
}

/* =============================================================================
   SPIN BOX
   ============================================================================= */

QSpinBox, QDoubleSpinBox {
    background-color: {{BACKGROUND}};
    border: 2px solid {{BORDER}};
    border-radius: 4px;
    padding: 8px;
    min-height: 40px;
}

QSpinBox:focus, QDoubleSpinBox:focus {
    border-color: {{SECONDARY}};
}

QSpinBox:disabled, QDoubleSpinBox:disabled {
    background-color: {{SURFACE_VARIANT}};
    color: {{TEXT_DISABLED}};
}

QSpinBox::up-button, QDoubleSpinBox::up-button {
    subcontrol-origin: border;
    subcontrol-position: top right;
    width: 20px;
    border-left: 2px solid {{BORDER}};
    background-color: {{SURFACE_VARIANT}};
}

QSpinBox::up-button:hover, QDoubleSpinBox::up-button:hover {
    background-color: {{BORDER}};
}

QSpinBox::down-button, QDoubleSpinBox::down-button {
    subcontrol-origin: border;
    subcontrol-position: bottom right;
    width: 20px;
    border-left: 2px solid {{BORDER}};
    border-top: 1px solid {{BORDER}};
    background-color: {{SURFACE_VARIANT}};
}

QSpinBox::down-button:hover, QDoubleSpinBox::down-button:hover {
    background-color: {{BORDER}};
}

/* =============================================================================
   CHECKBOX & RADIO
   ============================================================================= */

QCheckBox, QRadioButton {
    spacing: 8px;
}

QCheckBox::indicator, QRadioButton::indicator {
    width: 20px;
    height: 20px;
    border-radius: 4px;
    border: 2px solid {{TEXT_SECONDARY}};
    background-color: {{BACKGROUND}};
}

QCheckBox::indicator:checked, QRadioButton::indicator:checked {
    background-color: {{PRIMARY}};
    border-color: {{PRIMARY}};
    image: url(data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHZpZXdCb3g9IjAgMCAyMCAyMCIgeG1sbnM9Imh0dHA6Ly93d3cudzMub3JnLzIwMDAvc3ZnIj48cGF0aCBkPSJNNiAxMkwxNiA0IiBzdHJva2U9IndoaXRlIiBzdHJva2Utd2lkdGg9IjIiIGZpbGw9Im5vbmUiLz48L3N2Zz4=);
}

QCheckBox::indicator:checked:hover, QRadioButton::indicator:checked:hover {
    background-color: {{PRIMARY_DARK}};
}

QCheckBox::indicator:unchecked:hover, QRadioButton::indicator:unchecked:hover {
    border-color: {{PRIMARY}};
}

QCheckBox::indicator:disabled, QRadioButton::indicator:disabled {
    background-color: {{SURFACE_VARIANT}};
    border-color: {{BORDER}};
}

/* =============================================================================
   GROUP BOX
   ============================================================================= */

QGroupBox {
    border: 2px solid {{BORDER}};
    border-radius: 8px;
    margin-top: 16px;
    padding: 16px;
    font-weight: bold;
}

QGroupBox::title {
    subcontrol-origin: margin;
    left: 16px;
    padding: 0 8px;
    background-color: {{BACKGROUND}};
}

/* =============================================================================
   SCROLL BAR
   ============================================================================= */

QScrollBar:vertical {
    background-color: {{SURFACE_VARIANT}};
    width: 12px;
    border-radius: 6px;
}

QScrollBar::handle:vertical {
    background-color: {{BORDER_DARK}};
    border-radius: 6px;
    min-height: 30px;
}

QScrollBar::handle:vertical:hover {
    background-color: {{TEXT_DISABLED}};
}

QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical {
    border: none;
    background: none;
    height: 0px;
}

QScrollBar:horizontal {
    background-color: {{SURFACE_VARIANT}};
    height: 12px;
    border-radius: 6px;
}

QScrollBar::handle:horizontal {
    background-color: {{BORDER_DARK}};
    border-radius: 6px;
    min-width: 30px;
}

QScrollBar::handle:horizontal:hover {
    background-color: {{TEXT_DISABLED}};
}

QScrollBar::add-line:horizontal, QScrollBar::sub-line:horizontal {
    border: none;
    background: none;
    width: 0px;
}

/* =============================================================================
   PROGRESS BAR
   ============================================================================= */

QProgressBar {
    background-color: {{BORDER}};
    border: none;
    border-radius: 4px;
    height: 8px;
    text-align: center;
}

QProgressBar::chunk {
    background-color: {{PRIMARY}};
    border-radius: 4px;
}

QProgressBar::chunk:disabled {
    background-color: {{BORDER_DARK}};
}

/* =============================================================================
   TAB WIDGET
   ============================================================================= */

QTabWidget::pane {
    border: 2px solid {{BORDER}};
    border-radius: 8px;
    background-color: {{BACKGROUND}};
}

QTabBar::tab {
    background-color: {{SURFACE_VARIANT}};
    border: 2px solid {{BORDER}};
    border-bottom: none;
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
    padding: 12px 24px;
    margin-right: 4px;
    font-weight: bold;
}

QTabBar::tab:selected {
    background-color: {{BACKGROUND}};
    border-bottom: 2px solid {{BACKGROUND}};
}

QTabBar::tab:hover:!selected {
    background-color: {{SURFACE}};
}

QTabBar::tab:disabled {
    color: {{TEXT_DISABLED}};
}

/* =============================================================================
   TABLE
   ============================================================================= */

QTableView, QTableWidget {
    background-color: {{BACKGROUND}};
    border: 2px solid {{BORDER}};
    border-radius: 8px;
    gridline-color: {{BORDER}};
}

QTableView::item, QTableWidget::item {
    padding: 8px;
    border: none;
}

QTableView::item:selected, QTableWidget::item:selected {
    background-color: {{SECONDARY}};
    color: {{TEXT_PRIMARY}};
}

QTableView::item:hover, QTableWidget::item:hover {
    background-color: {{SURFACE_VARIANT}};
}

QHeaderView::section {
    background-color: {{SURFACE_VARIANT}};
    color: {{TEXT_PRIMARY}};
    padding: 8px;
    border: none;
    border-right: 2px solid {{BORDER}};
    border-bottom: 2px solid {{BORDER}};
    font-weight: bold;
}

QHeaderView::section:hover {
    background-color: {{SURFACE}};
}

/* =============================================================================
   STATUS BADGE (Custom Widget)
   ============================================================================= */

StatusBadge {
    padding: 4px 12px;
    border-radius: 4px;
    font-weight: bold;
    font-size: 11px;
}

/* =============================================================================
   DIALOG
   ============================================================================= */

QDialog {
    background-color: {{BACKGROUND}};
}

/* =============================================================================
   MENU
   ============================================================================= */

QMenu {
    background-color: {{BACKGROUND}};
    border: 2px solid {{BORDER}};
    border-radius: 8px;
    padding: 8px;
}

QMenu::item {
    padding: 8px 16px;
    border-radius: 4px;
}

QMenu::item:selected {
    background-color: {{SURFACE_VARIANT}};
}

QMenu::separator {
    height: 2px;
    background-color: {{BORDER}};
    margin: 8px 0;
}

QMenu::indicator {
    width: 16px;
    height: 16px;
    left: 8px;
}

/* =============================================================================
   SLIDER
   ============================================================================= */

QSlider::groove:horizontal {
    height: 4px;
    background: {{BORDER}};
    border-radius: 2px;
}

QSlider::handle:horizontal {
    background: {{PRIMARY}};
    width: 20px;
    height: 20px;
    margin: -8px 0;
    border-radius: 10px;
}

QSlider::handle:horizontal:hover {
    background: {{PRIMARY_DARK}};
}

QSlider::groove:vertical {
    width: 4px;
    background: {{BORDER}};
    border-radius: 2px;
}

QSlider::handle:vertical {
    background: {{PRIMARY}};
    width: 20px;
    height: 20px;
    margin: 0 -8px;
    border-radius: 10px;
}

QSlider::handle:vertical:hover {
    background: {{PRIMARY_DARK}};
}

/* =============================================================================
   TOOL TIP
   ============================================================================= */

QToolTip {
    background-color: {{BACKGROUND}};
    color: {{BACKGROUND}};
    border: none;
    border-radius: 4px;
    padding: 4px 8px;
    font-size: 12px;
}

/* =============================================================================
   SCROLL AREA
   ============================================================================= */

QScrollArea {
    border: none;
    background-color: transparent;
}

QScrollArea > QWidget > QWidget {
    background-color: transparent;
}

/* =============================================================================
   FRAME
   ============================================================================= */

QFrame {
    background-color: transparent;
}

QFrame[frameShape="4"], QFrame[frameShape="5"] {
    /* HLine = 4, VLine = 5 */
    color: {{BORDER}};
}
